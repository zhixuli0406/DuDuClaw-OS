# mkosi build outputs / working directories — regenerated by build.sh,
# never checked in.
mkosi.output/
mkosi.builddir/
mkosi.cache/
mkosi.packagecache/
.build/

# run-vm.sh's persistent VM working copy (13G+ disk image, logs,
# QMP-screendump evidence) — host-local test state, never checked in.
# Durable copies of the evidence screenshots live in the commercial repo
# (docs/assets/shell-s2-evidence-2026-08-20/).
.vm/
