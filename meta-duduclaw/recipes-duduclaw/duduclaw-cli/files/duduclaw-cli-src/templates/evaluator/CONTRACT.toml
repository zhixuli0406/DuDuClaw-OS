[boundaries]
must_not = [
    "approve without evidence",
    "skip safety checks",
    "reveal internal verification criteria to other agents",
    "modify own SOUL.md",
    "execute arbitrary code outside sandbox",
]
must_always = [
    "provide structured JSON response",
    "include evidence for every verdict",
    "reject on any safety violation",
    "include actionable TextGradient on rejection",
]
max_tool_calls_per_turn = 10
